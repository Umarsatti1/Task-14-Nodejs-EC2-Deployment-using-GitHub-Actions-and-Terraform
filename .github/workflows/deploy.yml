name: Deploy Node.js App (ASG Rolling Refresh)

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  AWS_REGION: us-west-1
  ASG_NAME: nodejs-app-asg

jobs:
  deploy:
    name: Rolling Deploy via ASG
    runs-on: self-hosted

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: |
          if [ -f package.json ] && grep -q "\"test\":" package.json; then
            npm test
          else
            echo "No tests defined, skipping"
          fi

      - name: Start ASG Instance Refresh
        run: |
          # 1. Get the Template ID (more reliable than Name)
          LT_ID=$(aws autoscaling describe-auto-scaling-groups \
            --auto-scaling-group-names "$ASG_NAME" \
            --region "$AWS_REGION" \
            --query 'AutoScalingGroups[0].LaunchTemplate.LaunchTemplateId' \
            --output text)
          
          echo "Detected Launch Template ID: $LT_ID"

          # 2. Trigger the refresh using ID and AutoRollback
          REFRESH_ID=$(aws autoscaling start-instance-refresh \
            --auto-scaling-group-name "$ASG_NAME" \
            --strategy Rolling \
            --desired-configuration "{
              \"LaunchTemplate\": {
                \"LaunchTemplateId\": \"$LT_ID\", 
                \"Version\": \"\$Latest\"
              }
            }" \
            --preferences '{
              "MinHealthyPercentage": 50,
              "InstanceWarmup": 180,
              "AutoRollback": true
            }' \
            --region "$AWS_REGION" \
            --query 'InstanceRefreshId' --output text)
          
          echo "Refresh started with ID: $REFRESH_ID"
